#include <stdio.h>
/**
 * main - A programthat prints all single digit numbers of base 10
 * without using char
 * Return: 0 (Success)
 */
int main(void)
{
	int i = 48;

	for (i = 48; i < 58; i++)
		putchar(i);
		putchar('\n');
	return (0);
}
