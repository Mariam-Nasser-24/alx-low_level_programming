#include "main.h"
/**
 * mul - A function that multiplies two integers
 * @a: the first number
 * @b: the second number
 * Return: the result
 */
int mul(int a, int b)
{
	int r;

	r = a * b;
	return (r);
}
